{{!
  Template for index.js
}}/**
 * {{desc}}{{#if module}}
 * @module {{module}}{{/if}}{{#each parentmodule}}
 * @see module:{{this}}{{/each}}
 */

'use strict'


{{#each modules}}const {{#if isSubclass}}{{pascalcase name}}{{else}}{{name}}{{/if}} = require('./{{requireName}}')
{{/each}}

{{#each modules}}
exports.{{#if isSubclass}}{{pascalcase name}} = {{pascalcase name}}{{else}}{{name}} = {{name}}{{/if}}
{{/each}}

module.exports = {
{{#each modules}}  {{#if isSubclass}}{{pascalcase name}}{{else}}{{name}}{{/if}}{{#if @last}}{{else}},
{{/if}}{{/each}}
}
