{{!
  Template for index.js
}}/**
 * {{desc}}{{#if module}}
 * @module {{module}}{{/if}}{{#each parentmodule}}
 * @see module:{{this}}{{/each}}
 */

'use strict'

module.exports = {
{{#each modules}}{{#if isSubmodules}}  /**
   * @name {{camelcase name}}
   */
{{/if}}  {{#if isSubclass}}/**
   * @name {{pascalcase name}}
   */
  get {{pascalcase name}}{{else}}get {{name}}{{/if}} () { return require('./{{requireName}}') }{{#if @last}}{{else}},
{{/if}}{{/each}}
}
