{{!
  Template for index.js
}}/**
 * {{desc}}{{#if module}}
 * @module {{module}}{{/if}}{{#each parentmodule}}
 * @see module:{{this}}{{/each}}
 */

'use strict'

let d = (module) => module && module.default || module

module.exports = {
{{#each modules}}  get {{#if isSubclass}}{{pascalcase name}}{{else}}{{name}}{{/if}} () { return d(require('./{{requireName}}')) }{{#if @last}}{{else}},
{{/if}}{{/each}}
}
